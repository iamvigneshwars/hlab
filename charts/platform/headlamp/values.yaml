headlamp:
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - host: headlamp.visar.cloud
        paths:
          - path: /
            pathType: Prefix
    tls:
      - hosts:
          - headlamp.visar.cloud
