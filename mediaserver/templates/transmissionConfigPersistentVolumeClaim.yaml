apiVersion: v1
kind: PersistentVolume 
metadata:
  name: transmission-config
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain 
  storageClassName: local-storage
  hostPath:
    path: /kubeconf/transmission-config

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: transmission-config
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: local-storage
  volumeName: transmission-config
  resources:
    requests:
      storage: 1Gi
